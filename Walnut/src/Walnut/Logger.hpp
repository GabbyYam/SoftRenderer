#pragma once

#include <glm/fwd.hpp>
#include <glm/glm.hpp>
#include <spdlog/spdlog.h>
namespace utils {

    void debugColor(const glm::vec3& color);
    void PrintVector(const glm::vec3& v);
}  // namespace utils